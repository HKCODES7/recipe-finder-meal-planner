import {browser} from '$app/environment';import type{Recipe,MealPlan,Day}from './types';
const days:Day[]=['monday','tuesday','wednesday','thursday','friday','saturday','sunday'];
function read<T>(key:string,fallback:T):T{if(!browser)return fallback;try{return JSON.parse(localStorage.getItem(key)||'null')??fallback}catch{return fallback}}
let favorites=$state<Recipe[]>(read('favorites',[]));let customRecipes=$state<Recipe[]>(read('customRecipes',[]));let mealPlan=$state<MealPlan>(read('mealPlan',Object.fromEntries(days.map(d=>[d,null])) as MealPlan));
if(browser){$effect.root(()=>{$effect(()=>localStorage.setItem('favorites',JSON.stringify(favorites)));$effect(()=>localStorage.setItem('customRecipes',JSON.stringify(customRecipes)));$effect(()=>localStorage.setItem('mealPlan',JSON.stringify(mealPlan)))})}
export const appState={get favorites(){return favorites},get customRecipes(){return customRecipes},get mealPlan(){return mealPlan},isFavorite:(id:string)=>favorites.some(r=>r.id===id),toggleFavorite(r:Recipe){favorites=favorites.some(x=>x.id===r.id)?favorites.filter(x=>x.id!==r.id):[...favorites,r]},saveRecipe(r:Recipe){customRecipes=customRecipes.some(x=>x.id===r.id)?customRecipes.map(x=>x.id===r.id?r:x):[...customRecipes,r]},deleteRecipe(id:string){customRecipes=customRecipes.filter(x=>x.id!==id);favorites=favorites.filter(x=>x.id!==id);for(const d of days)if(mealPlan[d]?.id===id)mealPlan={...mealPlan,[d]:null}},setMeal(day:Day,r:Recipe|null){mealPlan={...mealPlan,[day]:r}}};
